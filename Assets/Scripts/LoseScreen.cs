using UnityEngine;
using UnityEngine.SceneManagement;

public class LoseScreen : MonoBehaviour
{
    public static LoseScreen loseScreen;

   /* public void Lose()
    {
        Debug.Log("Victor: entrada a la funcion Lose");
        if (GameController.gameController.vidas <= 0)
        {
            Debug.Log("Victor: Se detectó que no hay vidas");
            gameObject.SetActive(true);
        }
    }

    public void Retry()
    {

        SceneManager.LoadScene("Escena principal");

    }

    public void Exit()
    {
        Debug.Log("Victor: Salir del juego");
        Application.Quit();
    }
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Awake()
    {
        gameObject.SetActive(true);
        gameObject.SetActive(false);
    }

    void Start()
    {
        
       // Lose();

    }

    // Update is called once per frame
    void Update()
    {
        
    }*/
}
